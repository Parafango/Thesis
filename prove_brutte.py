import numpy as np
import pandas as pd
from scipy.interpolate import NearestNDInterpolator


a = np.array([2, 4, 6, 7])

b = 2

c = np.sqrt(np.add(np.square(a), np.square(b)))

print(c)

palla = 'palla'

carta = 'palla'

print(palla)

print(carta)